{{- if .Values.kc.enabled }}
apiVersion: v1
kind: Secret

metadata:
  name: {{ .Chart.Name }}-postgres-secret
  labels:
    app: {{ .Chart.Name }}

data:
{{- range $key, $value := .Values.postgresDbSecret.data }}
  {{ $key }}: {{ $value | b64enc | quote }}
{{- end }}
{{- end }}

type: {{ .Values.postgresDbSecret.type }}