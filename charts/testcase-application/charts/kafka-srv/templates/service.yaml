{{- if .Values.kafka-srv.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  ports:
  - port: 5089
    targetPort: 5089
  selector:
    app: {{ .Chart.Name }}
{{- end }}
